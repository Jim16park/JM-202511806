package pe.edu.upeu.clinica.enums;

// Día de la semana en abreviatura de 3 letras (es), usado en upeu_horario.
// La BD valida el dominio con un CHECK CONSTRAINT (chk_dia_semana).
public enum DiaSemana {
    LUN, MAR, MIE, JUE, VIE, SAB, DOM
}
